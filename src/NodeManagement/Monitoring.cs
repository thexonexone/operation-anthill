using System;
namespace NodeManagement {
    public class Monitoring {
        public static void MonitorCpuUsage(string nodeId) {
            if (nodeId == "10.0.0.2") {
                // Code to monitor and optimize CPU usage
                Console.WriteLine("Monitoring CPU usage for node: " + nodeId);
            }
        }
    }
}